<Window x:Class="StageManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StageBar"
        Icon="Assets/stagebar.ico"
        Width="286"
        Height="560"
        MinHeight="320"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Opacity="1">

  <Grid Background="Transparent">
    <ScrollViewer x:Name="PreviewScrollViewer"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Background="Transparent"
                  BorderThickness="0"
                  VerticalScrollBarVisibility="Hidden"
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly"
                  CanContentScroll="False"
                  PreviewMouseWheel="PreviewScrollViewer_PreviewMouseWheel"
                  ScrollChanged="PreviewScrollViewer_ScrollChanged">
      <Grid Background="Transparent">
        <StackPanel x:Name="PreviewList" Background="Transparent" />
        <TextBlock x:Name="EmptyState"
                   Text="No open windows"
                   Visibility="Collapsed"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="#CCFFFFFF"
                   FontSize="12" />
      </Grid>
    </ScrollViewer>
  </Grid>
</Window>
